# Compiled output
*.exe
*.o
*.obj

# Pipeline intermediate files (regenerated by pipeline.sh each time)
frames/
frames-resized/

# Root-level leftover spritesheets (not part of the animations/ structure)
/spritesheet.png
/spritesheet.json

# SDL2 library (removed from the project, no longer used)
lib/SDL2/

# Source art — ico is what the app uses at runtime
bird-icon.png

# Claude Code local settings
.claude/

# Session/AI context files (local only)
CLAUDE.md
HANDOFF.md
